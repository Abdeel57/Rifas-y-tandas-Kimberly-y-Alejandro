# 🔐 Variables de Entorno - Rifas y tandas Kimberly y Alejandro
# Copia este archivo a .env y rellena con tus valores reales

# Base de Datos PostgreSQL
# Obtén esta URL desde Railway después de crear la base de datos PostgreSQL
DATABASE_URL=postgresql://usuario:password@host:puerto/database?schema=public

# Configuración del Servidor
NODE_ENV=development
PORT=3000

# Seguridad - GENERA UN SECRET ÚNICO Y SEGURO
# Ejecuta: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=genera_un_secret_unico_y_seguro_aqui

# CORS (URLs permitidas - se configuran en backend/src/main.ts)
# Los dominios se agregan directamente en el código, no aquí
